<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Index</title>
</head>

<body>
    <% if (isLoggedIn) { %>
        <h1>Hello <%= username %>!</h1>
        <button onclick="location.href='/logout'">logout</button>
        <button onclick="location.href='/members'">Go to member page</button>
        <% } else { %>
            <h1>Welcome!</h1>
            <button onclick="location.href='/login'">log in</button>
            <button onclick="location.href='/signup'">signup</button>
            <% } %>
</body>

</html>