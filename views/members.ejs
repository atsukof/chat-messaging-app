<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Members</title>
</head>
<body>
  <h1>Hello, <%= username %>.</h1>
  <div>
      <img src="/chatting.jpeg" alt="Generated Ukiyoe Image" style="max-width:400px;">
</div>

<button onclick="location.href='/create_room'">Create new room</button>
  
<p>Your chat rooms</p>
<table border="1" cellspacing="0" cellpadding="5">
  <thead>
    <tr>
      <th>Room Name</th>
      <th>Last Message Sent Datetime</th>
      <th>Number of Unread Messages</th>
    </tr>
  </thead>
  <tbody>
    <% if (rooms && rooms.length> 0) { %>
      <% rooms.forEach(room=> { %>
        <tr>
          <td>
            <a href="/room/<%= room.room_id %>">
              <%= room.room_name %>
            </a>
          </td>
          <td>
            <%= room.latest_message_datetime %>
          </td>
          <td>
            <%= room.unread_count %>
          </td>
        </tr>
        <% }); %>
          <% } else { %>
            <tr>
              <td colspan="3">No rooms available.</td>
            </tr>
            <% } %>
  </tbody>
</table>

  <br />
  <button onclick="location.href='/logout'">sign out</button>
</body>
</html>