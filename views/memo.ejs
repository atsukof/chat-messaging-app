<!-- ※以下、必要に応じてクライアントサイドのJavaScriptで [+]・[-] ボタンの動作を実装 -->
<!-- <script>
            // 例: ボタンをクリックしたら、AJAXでリアクション追加/削除するなどの処理を実装できます。
            document.querySelectorAll('.reaction-btn').forEach(function (btn) {
                btn.addEventListener('click', function (e) {
                    e.preventDefault();
                    let messageId = this.getAttribute('data-message-id');
                    let emojiId = this.getAttribute('data-emoji-id');
                    // ボタンのクラスに応じて追加か削除か判断
                    if (this.classList.contains('add-reaction')) {
                        // リアクション追加の処理（例: fetch APIでPOSTリクエスト送信）
                        console.log('Add reaction:', messageId, emojiId);
                    } else if (this.classList.contains('remove-reaction')) {
                        // リアクション削除の処理
                        console.log('Remove reaction:', messageId, emojiId);
                    }
                });
            });
        </script> -->